node_modules/
dist/
.next/
next-env.d.ts
*.tsbuildinfo
*.db
*.db-wal
*.db-shm
# ad-hoc test-run artifacts (a bad path once created a literal undefined/ dir)
undefined/
.ctxvault/
.env
.env.local
.DS_Store
.vercel/
# docs/assets/ is tracked: the README's hero GIF lives there, and an
# ignored asset is a broken image on GitHub.

# local AI-tool config (machine-specific paths; each dev registers their own)
.claude/
.mcp.json
